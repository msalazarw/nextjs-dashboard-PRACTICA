// app/seed/definitions.ts
export interface Revenue {
  amount: number;
  date: string;
  revenue: number; // Añade esta línea si 'revenue' es una propiedad de los objetos que manejas.
  // otros campos según tus necesidades
}
